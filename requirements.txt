flask
flask-cors

numpy
joblib
scikit-learn

tensorflow

# pad_sequences is in keras.preprocessing in TF 2.x
# so standalone keras is not strictly required, but you can pin it if needed:
# keras==3.5.0
